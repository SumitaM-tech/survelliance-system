# For local dev — leave empty (proxy handles it via package.json)
REACT_APP_API_URL=

# For production (Vercel/Netlify) — set to your Render backend URL
# REACT_APP_API_URL=https://your-app.onrender.com
